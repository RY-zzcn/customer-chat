version: '3.8'

services:
  chat:
    build: .
    container_name: customer-chat
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
